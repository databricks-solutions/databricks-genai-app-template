# Next.js Development Environment Variables
# This file is ONLY loaded during development (next dev)
# Not loaded during production builds

# Backend API URL - For client-side direct calls in development
# Development: Call backend directly to bypass Next.js dev server buffering on streaming responses
# This allows the frontend to receive streaming responses without interference
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
